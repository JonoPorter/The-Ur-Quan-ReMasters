using System;
using System.Collections.Generic;
using System.IO;

namespace ReMasters.SuperMelee
{
    public static class SuperMeleePaths
    {
        public static readonly string ShipDir = System.Environment.CurrentDirectory + Path.DirectorySeparatorChar + "Ships" + Path.DirectorySeparatorChar;
        public static readonly string FleetDir = System.Environment.CurrentDirectory + Path.DirectorySeparatorChar + "Fleets" + Path.DirectorySeparatorChar;
        public static readonly string ConfigDir  = System.Environment.CurrentDirectory + Path.DirectorySeparatorChar + "Config" + Path.DirectorySeparatorChar;
        public static readonly string SoundDir = System.Environment.CurrentDirectory + Path.DirectorySeparatorChar + "Sounds" + Path.DirectorySeparatorChar;
        public static readonly string MusicDir = System.Environment.CurrentDirectory + Path.DirectorySeparatorChar + "Music" + Path.DirectorySeparatorChar;
    
    }
}
